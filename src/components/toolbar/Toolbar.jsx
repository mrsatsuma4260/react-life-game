import React from 'react'

import styles from './Toolbar.module.css'

const Toolbar = () => {
    return (
        <toolbar className={styles.lifeToolbar}>
            
        </toolbar>
    );
}

export default Toolbar;