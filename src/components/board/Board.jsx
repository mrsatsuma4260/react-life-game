import React from 'react'

import styles from './Board.module.css'

const Board = () => {
    return (
        <board className={styles.lifeBoard}>
            
        </board>
    );
}

export default Board;